project Common is

   for Source_Dirs use ();

   package Compiler is
      for Default_Switches ("ada") use ("-gnato", "-gnatwaeCYUKWMJ", "-O1", "-fstack-check", "-gnat95", "-gnata", "-g");
   end Compiler;

   package Builder is
      for Default_Switches ("ada") use ("-O1");
   end Builder;
end Common;
